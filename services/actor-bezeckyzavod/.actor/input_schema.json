{
  "title": "Bezeckyzavod.cz Scraper Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "supabaseUrl": {
      "title": "Supabase URL",
      "type": "string",
      "description": "URL of the Supabase project (e.g. https://xxx.supabase.co)",
      "editor": "textfield"
    },
    "supabaseServiceKey": {
      "title": "Supabase Service Role Key",
      "type": "string",
      "description": "Service role key for Supabase (has write access)",
      "editor": "textfield",
      "isSecret": true
    },
    "maxPages": {
      "title": "Max Pages per Category",
      "type": "integer",
      "description": "Maximum number of pagination pages to scrape per category",
      "default": 100,
      "minimum": 1,
      "editor": "number"
    },
    "proxyConfig": {
      "title": "Proxy configuration",
      "type": "object",
      "description": "Select proxies to be used by this actor.",
      "editor": "proxy",
      "prefill": { "useApifyProxy": true },
      "default": { "useApifyProxy": true }
    }
  }
}
